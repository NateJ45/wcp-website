---
import Section from '@/components/Section.astro';
import SectionHeader from '@/components/SectionHeader.astro';
import StepList from '@/components/StepList.astro';
import { emphasisHtml } from '@/lib/emphasis';
import {
  titleIdFor,
  eyebrowTone,
  bandSize,
  type SectionData,
  headerLeadHtml,
} from './section-helpers';

interface Step {
  title: string;
  body?: string;
  /** The body's rich twin (bold / italic). Empty on every stored step. */
  bodyRich?: unknown;
  note?: string;
}
interface Props {
  section: SectionData;
}
const { section } = Astro.props;
const titleId = titleIdFor(section);
const steps = ((section.steps as Step[] | undefined) ?? []).map((s) => ({
  title: s.title,
  body: s.body ?? '',
  bodyHtml: emphasisHtml(s.bodyRich),
  note: s.note,
}));
---

<Section bg={section.background} seam={section.seam} size={bandSize(section)} labelledby={titleId}>
  {
    section.header?.title && (
      <SectionHeader
        leadHtml={headerLeadHtml(section.header)}
        accent={section.header.headingAccent}
        eyebrow={section.header.eyebrow}
        title={section.header.title}
        lead={section.header.lead}
        align={section.header.align}
        titleId={titleId}
        eyebrowTone={eyebrowTone(section.background)}
      />
    )
  }
  <div class="mt-12">
    <StepList steps={steps} footnote={section.footnote as string | undefined} />
  </div>
</Section>
