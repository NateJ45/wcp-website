---
/* Twos + Threes now share one hub page. Keep this URL working (bookmarks,
   the home class cards) by redirecting to the combined page. */
export const prerender = false;
return Astro.redirect('/family-hub/twos-threes', 301);
---
