---
// The two Pre-K classes merged onto one hub page (same teacher, same
// handbook) — old bookmarks land there.
export const prerender = false;
return Astro.redirect('/family-hub/pre-k', 301);
---
