---
/* ============================================================================
   Family Hub — Pre-K PM Classroom (GATED, page-builder)
   ============================================================================
   Fixed widget: class facts + pay button + class-notes (class notes are their
   own editable docs), via <ClassHubBody>. Editable heading/intro + any extra
   sections come from the class's hubPage doc (hubKey "pre-k-pm"); falls back to
   the built-in strings if no doc exists yet.
   ============================================================================ */
export const prerender = false;

import HubShell from '@/layouts/HubShell.astro';
import ClassHubBody from '@/components/hub/ClassHubBody.astro';
import { sanityFetch } from '@/lib/sanity';
import { HUB_PAGE_QUERY } from '@/lib/queries';

interface HubPageDoc {
  heading?: string;
  intro?: string;
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  sections?: any[];
}
let doc: HubPageDoc | null = null;
try {
  doc = await sanityFetch<HubPageDoc | null>(HUB_PAGE_QUERY, { key: 'pre-k-pm' });
} catch {
  doc = null;
}
const title = doc?.heading || 'Pre-K PM Classroom';
const intro = doc?.intro || 'Kindergarten readiness on an afternoon schedule.';
---

<HubShell
  title={title}
  intro={intro}
  current="/family-hub/pre-k-pm"
  pageTitle="Pre-K PM Classroom — WCP Family Hub"
>
  <ClassHubBody slug="pre-k-pm" sections={doc?.sections} />
</HubShell>
