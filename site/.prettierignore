dist
node_modules
.astro
.wrangler
package-lock.json
public
playwright-report
test-results
src/lib/lucide-icons.ts
src/lib/brand-icons.ts

# Generated idempotency caches from the migration scripts (also gitignored).
scripts/.asset-map.json
scripts/.dir-photos.json

# Sanity typegen intermediate (also gitignored). Machine-written JSON.
schema.json

# Rendered-HTML parity baselines (scripts/page-parity.mjs). They are byte
# snapshots of built pages; prettier must never rewrite them.
scripts/.parity

# Generated by `npm run studio:dev` (npx sanity dev); gitignored runtime shim.
.sanity

# Saved Family Hub session written by tests/hub-auth.setup.ts (also gitignored).
# Playwright's own artifact, and it holds a live session cookie.
tests/.auth
